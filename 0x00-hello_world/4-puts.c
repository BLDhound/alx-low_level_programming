#include <stdio.h>

/**
 * main -  prints the phrase programming is like building a multilingual puzzle
 *
 * Return: 0 if exited well, nonzero if otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
