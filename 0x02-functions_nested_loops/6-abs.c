#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @c: integer used in the argument
 * Return: absolute value if int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
